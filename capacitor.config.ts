import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'activity5-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
